# Ordinal webhook samples

Sample webhook deliveries for [Ordinal](https://tryordinal.com), the
social media planning tool. Ordinal's webhook feature
([docs](https://docs.tryordinal.com/integrations/webhooks/introduction))
shipped 2026-04-07 and covers the full post lifecycle, social-profile
connection state, approvals, and workspace invites.

See [`scripts/ordinal/`](../../scripts/ordinal/) for the generator that
produces these payloads from Ordinal's docs.

## Delivery format

Every event shares one envelope:

```json
{ "type": "post.published", "data": { ... }, "createdAt": "2025-02-26T14:30:00.000Z" }
```

- `type` — the event identifier; this is the `topic_identifier` in
  [`index.json`](./index.json), so each delivery is filed as
  `latest/<type>.json`.
- `data` — event-specific; schema varies by `type`.
- `createdAt` — ISO 8601 emission time.

Ordinal does not document a webhook signature/HMAC scheme; the unguessable
delivery URL acts as the shared secret, and custom headers can be attached
per subscription via the webhook's `headers` field if downstream
verification is needed.

## ⚠️ Provenance of these payloads

> **All payloads in `latest/` are docs-sourced and generated by a script.**
> Ordinal has ~20 event types and no test-event trigger, and most events
> can't be produced through the API unattended (OAuth connects, real
> publishes, approver/invitee actions, the debounced in-app editor), so a
> live capture of the full set isn't practical. Instead,
> [`scripts/ordinal/docs.ts`](../../scripts/ordinal/docs.ts) scrapes
> Ordinal's documentation: it discovers every event page from the docs
> index (`llms.txt`) and extracts the canonical example payload each page
> publishes in a `json` fence, then writes it here in this repo's
> `{ headers, body, topic }` shape. Regenerate any time with:
>
> ```sh
> yarn generate:ordinal
> ```
>
> These are **not** live captures. They use Ordinal's own placeholder data
> (`Acme Inc`, `jane@example.com`, `550e8400-…` UUIDs) so they can't be
> mistaken for real account traffic, and `headers` are representative, not
> recorded (the docs don't enumerate delivery headers).

## Event taxonomy

| `type` | Group |
|--------|-------|
| `post.created` | Post |
| `post.scheduled` | Post |
| `post.rescheduled` | Post |
| `post.unscheduled` | Post |
| `post.published` | Post |
| `post.publish_failed` | Post |
| `post.archived` | Post |
| `post.permanently_deleted` | Post |
| `post.content.edited` | Post |
| `post.comment.created` | Post |
| `post.inline_comment.created` | Post |
| `post.approval.requested` | Approval |
| `post.approval.approved` | Approval |
| `campaign.approval.requested` | Approval |
| `campaign.approval.approved` | Approval |
| `invite.created` | Invite |
| `invite.accepted` | Invite |
| `social_profile.connected` | Social profile |
| `social_profile.disconnected` | Social profile |
| `social_profile.reconnect_needed` | Social profile |

### Field notes worth knowing

- `channels` (array) appears on the editorial events (created, scheduled,
  rescheduled, unscheduled, archived, content.edited, permanently_deleted);
  `post.published` / `post.publish_failed` use a singular `channel` string
  because they fire once per channel.
- `linkedIn` / `x` content blocks (and `labels`) are present and nullable
  on the editorial events, absent on the publish-outcome events.
- The three `social_profile.*` events share one `profile` sub-schema,
  differing only in the actor/timestamp fields.

## Regenerating

```sh
yarn generate:ordinal
```

Rebuilds every payload from Ordinal's published docs. No credentials
needed. See [`scripts/ordinal/README.md`](../../scripts/ordinal/README.md)
for how it works and how to handle the docs adding or removing an event.
